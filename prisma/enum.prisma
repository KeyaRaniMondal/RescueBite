enum Role {
    ADMIN
    PROVIDER
    RECEIVER
}

enum UserStatus {
    PENDING
    ACTIVE
    RESTRICTED
    UNACTIVE
    BLOCKED
    DELETED
}

enum AuthProvider {
    GOOGLE
    CREDENTIAL
}

enum BusinessType {
    RESTAURANT
    BAKERY
    CAFE
    GROCERY
    OTHER
}
